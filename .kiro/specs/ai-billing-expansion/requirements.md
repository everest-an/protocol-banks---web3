# AI 计费与微支付系统 - 需求文档

**版本**: 1.0  
**日期**: 2026-02-02  
**状态**: 规划中

## 1. 执行摘要

基于现有的 AI Agent API 基础，扩展 Protocol Banks 为完整的 **AI 代理计费基础设施**，支持：
- 动态流支付（按时长/按量计费）
- 会话密钥授权（免签名支付）
- AI 哨兵风控（异常熔断）
- HTTP 402 微支付网关

这将使 Protocol Banks 从"企业薪酬工具"升级为"Web3 可编程商业基础设施"。

---

## 2. 目标用户

### Persona A: AI 服务商
- **痛点**: 订阅制门槛高，无法按量收费
- **需求**: 按 Token/按秒计费，支持微支付
- **解决方案**: PB-Stream 微支付网关

### Persona B: DePIN 项目方
- **痛点**: 需要押金，结算周期长
- **需求**: 实时按使用量扣费
- **解决方案**: PB-Rail 动态流支付

### Persona C: Web3 企业 CFO
- **痛点**: AI Agent 费用失控，缺乏预算管理
- **需求**: 为 AI 设置预算和权限
- **解决方案**: PB-Guard 会话密钥 + Sentinel 风控

---

## 3. 核心功能需求

### 3.1 动态流支付引擎 (PB-Stream)

**用户故事**:
> 作为 Web3 租车公司，我需要根据用户驾驶时长动态扣费，无需用户每次停车都签名支付。

**验收标准**:
- [ ] 支持状态通道链下记账
- [ ] 达到阈值（时间/金额）自动上链结算
- [ ] 支持动态费率（高峰期 x1.5）
- [ ] 提供费率预言机 API
- [ ] 支持实时暂停/恢复计费

**技术要求**:
- 链下累积计费，减少 Gas 消耗
- 支持每秒级别的计费粒度
- 提供 Webhook 通知计费事件

### 3.2 会话密钥授权 (PB-Guard 增强)

**用户故事**:
> 作为 AI Agent 平台，我的用户的 AI 每天调用上千次 API，需要一次授权、自动扣款的机制。

**验收标准**:
- [ ] 支持创建临时会话密钥（Session Key）
- [ ] 可设置权限限制（合约白名单、金额上限、有效期）
- [ ] 支持静默支付（无弹窗）
- [ ] 提供会话密钥管理界面
- [ ] 支持批量创建会话密钥

**技术要求**:
- 基于 ERC-4337 账户抽象
- Session Key 权限验证在链上执行
- 支持多级权限控制

### 3.3 AI 哨兵风控 (Sentinel)

**用户故事**:
> 作为用户，我不敢给新 AI 网站授权自动扣款，怕它把我的钱扣光。

**验收标准**:
- [ ] 实时监控消费行为
- [ ] 异常检测（频率激增、金额异常）
- [ ] 自动熔断机制
- [ ] 多渠道告警（Telegram/Email/Webhook）
- [ ] 用户可自定义风控规则

**技术要求**:
- Go 规则引擎实现
- 支持基于历史数据的基准线学习
- 毫秒级响应时间

### 3.4 HTTP 402 微支付网关

**用户故事**:
> 作为 API 服务商，我需要按次向 AI 收取加密货币，而不是月付订阅。

**验收标准**:
- [ ] 拦截 API 请求，返回 402 Payment Required
- [ ] 验证支付后放行请求
- [ ] 支持微小金额（0.001 U）
- [ ] 提供 SDK/中间件（Node.js/Go）
- [ ] 支持批量结算（减少 Gas）

**技术要求**:
- 实现 HTTP 402 协议扩展
- 支持状态通道累积支付
- 提供标准化的支付响应格式

---

## 4. 产品架构升级

### 4.1 五层协议栈

| 层级 | 模块名 | 功能 | 现状 |
|------|--------|------|------|
| L1 | PB-Link | 身份认证 | ✅ 已有 (Auth) |
| L2 | PB-Guard | 授权管理 | 🟡 需增强 (Session Keys) |
| L3 | PB-Stream | 流支付通信 | ❌ 新增 |
| L4 | PB-Rail | 全链结算 | ✅ 已有 (Payments) |
| L5 | PB-Proof | 审计存证 | ✅ 已有 (Audit) |

### 4.2 新增数据模型

```prisma
// 会话密钥
model SessionKey {
  id           String   @id @default(uuid())
  agentId      String   // 关联到现有 Agent
  publicKey    String   @unique
  permissions  Json     // 权限配置
  limitAmount  Decimal  // 总限额
  currentUsage Decimal  @default(0)
  expiresAt    DateTime
  isFrozen     Boolean  @default(false)
  createdAt    DateTime @default(now())
}

// 流支付会话
model StreamSession {
  id           String   @id @default(uuid())
  sessionKeyId String
  serviceId    String   // 服务提供商 ID
  startTime    DateTime
  endTime      DateTime?
  ratePerUnit  Decimal  // 单位费率
  totalUnits   Decimal  @default(0)
  totalAmount  Decimal  @default(0)
  status       String   // active, paused, completed
}

// 微支付记录
model MicroPayment {
  id           String   @id @default(uuid())
  sessionId    String
  amount       Decimal
  timestamp    DateTime @default(now())
  settled      Boolean  @default(false)
  txHash       String?
}

// 风控事件
model SentinelEvent {
  id           String   @id @default(uuid())
  sessionKeyId String
  eventType    String   // anomaly_detected, circuit_breaker_triggered
  severity     String   // low, medium, high, critical
  details      Json
  action       String   // freeze, alert, block
  createdAt    DateTime @default(now())
}
```

---

## 5. 业务场景

### 场景 1: AI 公司按 Token 计费

1. 用户在 AI 公司官网点击"连接钱包"
2. 授权创建 Session Key（限额 50 U，有效期 30 天）
3. AI 调用 API 时，PB-Stream 网关验证 Session Key
4. 链下累积计费（每 0.01 U）
5. 达到 10 U 时自动上链结算
6. Sentinel 监控异常调用频率

### 场景 2: DePIN 租车动态扣费

1. 用户扫码租车，授权 Session Key
2. 开始计时，每分钟计费（基础费率 + 动态调整）
3. 用户还车，停止计费
4. 自动结算总费用
5. 生成 PDF 账单

### 场景 3: GPU 算力预付费

1. 企业为 AI Agent 充值 100 U
2. AI Agent 使用 GPU 算力
3. 按秒计费，自动扣款
4. 余额不足时自动停止服务
5. 企业收到余额告警

---

## 6. 非功能性需求

### 6.1 性能要求
- 微支付网关响应时间 < 200ms
- 支持 10,000+ 并发会话
- 状态通道累积 100 笔后批量结算

### 6.2 安全要求
- Session Key 权限最小化原则
- 所有敏感操作需签名验证
- Sentinel 实时监控，异常立即熔断

### 6.3 合规要求
- 所有计费记录可审计
- 支持生成合规报表
- 符合各地区反洗钱法规

---

## 7. 开发路线图

### Phase 1: 基础设施 (Month 1-2)
- [ ] 实现 Session Key 合约
- [ ] 开发 PB-Stream 计费引擎（Go）
- [ ] 创建会话密钥管理界面

### Phase 2: 核心功能 (Month 3-4)
- [ ] HTTP 402 微支付网关
- [ ] 状态通道链下记账
- [ ] Sentinel 规则引擎

### Phase 3: 生态集成 (Month 5-6)
- [ ] 发布 SDK（Node.js/Python/Go）
- [ ] 集成主流 AI 服务商
- [ ] 开放 API 市场

---

## 8. 成功指标

- 接入 10+ AI 服务商
- 处理 100,000+ 微支付交易
- Session Key 使用率 > 60%
- Sentinel 拦截异常率 < 1%

---

## 9. 风险与挑战

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 链下计费数据丢失 | 高 | Redis 持久化 + 定期同步 |
| Session Key 被盗用 | 高 | Sentinel 实时监控 + 熔断 |
| Gas 费波动 | 中 | 批量结算 + L2 方案 |
| 用户体验复杂 | 中 | 简化授权流程 + 教育文档 |

---

## 10. 附录

### 术语表
- **Session Key**: 临时授权密钥，用于免签名支付
- **State Channel**: 状态通道，链下累积交易
- **HTTP 402**: 支付请求协议
- **Sentinel**: AI 哨兵风控系统
- **Circuit Breaker**: 熔断机制

### 参考资料
- ERC-4337: Account Abstraction
- ERC-3009: Transfer With Authorization
- HTTP 402 Payment Required Specification
