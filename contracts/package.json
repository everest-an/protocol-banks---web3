{
  "name": "protocol-banks-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Protocol Banks batch transfer",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network arbitrum",
    "tron:compile": "node scripts/compile-tron-v2.js",
    "tron:deploy:nile": "node scripts/deploy-tron-vault-nile.js",
    "tron:test:nile": "node scripts/test-vault-nile.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.6.1",
    "hardhat": "^2.19.4",
    "solc": "^0.8.20",
    "tronweb": "^6.2.0"
  }
}
